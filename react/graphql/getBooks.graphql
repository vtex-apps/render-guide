query GetBooks($cursor: Int, $numberOfEntries: Int)  {
  books(cursor: $cursor, numberOfEntries: $numberOfEntries) {
    content {
      id
      name
      authors
    }
    pageinfo {
      cursor
      hasNextPage
    }
  }
}
